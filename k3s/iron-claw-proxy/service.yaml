apiVersion: v1
kind: Service
metadata:
  name: iron-claw-proxy
  namespace: iron-claw
spec:
  type: ClusterIP
  selector:
    app: iron-claw-proxy
  ports:
    - port: 8100
      targetPort: 8100
      protocol: TCP
